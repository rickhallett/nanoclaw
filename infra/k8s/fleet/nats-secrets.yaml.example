apiVersion: v1
kind: Secret
metadata:
  name: nats-auth
  namespace: halo-fleet
type: Opaque
stringData:
  # Generate with: openssl rand -base64 24
  HQ_PASS: "REPLACE_ME"
  ADVISOR_PASS: "REPLACE_ME"
