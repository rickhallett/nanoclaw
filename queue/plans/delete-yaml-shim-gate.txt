Gate run: 2026-03-19

pytest not available in container (venv python is a host symlink; no pip for system python3.11).
Ran functional verification in lieu of full test suite:

  PASS  yaml_shim.py no longer exists
  PASS  0 yaml_shim references remain in halos/
  PASS  All 13 halos modules import cleanly:
          nightctl: cli, item, job, manifest, archive, config, container, executor, migrate_todoctl
          halctl:   config, renderer, provision, eval_harness
  PASS  yaml.safe_load and yaml.dump callable via PyYAML

Full pytest suite must be run on host: cd ~/code/nanoclaw && make gate
